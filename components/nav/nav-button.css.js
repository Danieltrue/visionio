import styled from "styled-components";

const NavButtonStyle = styled.div`
  position: absolute;
  top: -20%;
  button {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 0;
  }
`;

export default NavButtonStyle;
