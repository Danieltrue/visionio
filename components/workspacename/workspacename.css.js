import styled from "styled-components";

const WorkspaceNameStyle = styled.div`
    margin-left: -100px;
    h6 {
        font-weight: 400;
        color: var(--outline-clr);
        font-size: 14px;
        font-family: var(--font-sans);
    }
`;


export default WorkspaceNameStyle