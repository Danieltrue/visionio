import styled from "styled-components";

const VariationStyle = styled.div`
    padding: 0.5rem 0;
    p.adj {
        font-size: 13px;
        font-weight: 500;
        color: var(--font-clr);
    }
`;

export default VariationStyle