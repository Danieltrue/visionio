import styled from 'styled-components';

const ProfileNavStyle = styled.div`
    width: 30px;
    height: 30px;
    background-color: var(--profile-nav-clr);
    border-radius: 50%;
    p {
        color: var(--profile-nav-text-clr);
        font-weight: bolder;
        font-size: 12px;
    }
`;

export default ProfileNavStyle