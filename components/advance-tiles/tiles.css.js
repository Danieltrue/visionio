import styled from "styled-components";

const TilesStyle = styled.div`
    border-bottom: 1px solid var(--border-clr);
    padding: 0.6rem 0;
    padding-bottom: 0.7rem;
    section {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    p.adj {
        font-size: 70%;
    }
`;

export default TilesStyle